version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      
      - npm install -f

  build:
    commands:
      
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: dist/angular-build  # Replace with actual app name
